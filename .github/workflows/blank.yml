name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Ansible-lint
      uses: ansible/ansible-lint-action@v4.1.0
      with:
        targets: "."
