---

dependency:
  name: galaxy

driver:
  name: docker

lint:
  name: yamllint
  options:
    config-file: molecule/default/yaml-lint.yml

platforms:
  - name: instance
    image: "centos:7"
    privileged: true

provisioner:
  name: ansible
  lint:
    name: ansible-lint

scenario:
  name: default
